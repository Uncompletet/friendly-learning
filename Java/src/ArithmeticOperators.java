
public class ArithmeticOperators {
    public static void main(String[] args) {

        /*

        // Arithmetic Operators

        // int x = 10;
        // int y = 3;
        // int z;

        // z = x + y; addition
        // z = x - y; subtraction
        // z = x * y; multiplication
        // z = x / y; division
        // z = x % y;  gives the remainder of any division. For example: 10 / 2 = 5 flat. No rest.

         */

        /*

        // Augmented Assignment Operators
        // 3/10 would have decimals, but we define it using int, instead of double

        int x = 10;
        int y = 3;

        // x += y; shortened version of saying: x = x + y;
        // x -= y; shortened version of saying: x = x - y;
        // x *= y; shortened version of saying: x = x * y;
        // x /= y; shortened version of saying: x = x / y;
        // x %= y; shortened version of saying: x = x % y;

         */

        /*


        // Increment and Decrement Operators

        int x = 1;

        // x++; // if you are adding only 1 onto a number (like counting), this does the same as x = x + 1;
        // x--; // if you are subtracting only 1 from a number (like counting), this does the same as x = x - 1;

            System.out.println(x);
         */


        /*

        // ORDER OF OPERATIONS [P-E-M-D-A-S]

        double result = 3 + 4 * (7 - 5) / 2.0;

        // P-E-M-D-A-S is the order it look at the calculations
        // so here it would be:
        // 1. (Parenthesis)     (7 - 5) = 2
        // 2. (Exponents)       none
        // 3. (Multiplication)  4 * 2 = 8
        // 4. (Division)        8 / 2.0 = 4
        // 5. (Addition)        3 + 4 = 7 (Result)
        // 6. (Subtractions)    none

        */

        // System.out.println(result);

    }
}
